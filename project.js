const id=new URLSearchParams(location.search).get('id');fetch('projects.json').then(r=>r.json()).then(items=>{const p=items.find(x=>x.id===id)||items[0];document.title=p.title+' | Jori Fahad Baaljahr';document.getElementById('project').innerHTML=`<section class="projectHero"><p class="kicker">${p.year} · AI Project</p><h1>${p.title}</h1><h2>${p.subtitle}</h2><div class="tags">${p.focus.map(x=>`<span>${x}</span>`).join('')}</div></section><div class="cover"></div>${[['Short Description',p.short],['Problem',p.problem],['Solution',p.solution],['Models & Technologies',p.models],['Demo','Demo content will be added here.'],['Results',p.results],['Code Highlights','Selected implementation details will be presented here.']].map(x=>`<section class="detail"><h3>${x[0]}</h3><p>${x[1]}</p></section>`).join('')}<div class="actions"><a class="button" href="${p.github}">View Full Code</a><a class="ghost" href="${p.drive}">View Project Files</a></div>`});